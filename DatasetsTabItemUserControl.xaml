<UserControl x:Class="DynamicTestingWPF.DatasetsTabItemUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:viewmodels="clr-namespace:DynamicTestingWPF.ViewModels"
             xmlns:local="clr-namespace:DynamicTestingWPF"
             mc:Ignorable="d" 
             d:DesignHeight="355" d:DesignWidth="800">
    <Grid>
        <TextBox HorizontalAlignment="Center" Text="{Binding TextBoxContent}" TextWrapping="Wrap" VerticalAlignment="Center" Width="120"/>
        <Button Content="Hide -from UserControl" Command="{Binding HidePanelCommand}" HorizontalAlignment="Left" Margin="182,0,0,0" VerticalAlignment="Center" Width="153"/>
        <Button Content="Show -from UserControl" Command="{Binding ShowPanelCommand}" HorizontalAlignment="Left" Margin="465,0,0,0" VerticalAlignment="Center" Width="153"/>
        <Button Content="Button" HorizontalAlignment="Center" Margin="0,132,0,0" VerticalAlignment="Top" Click="Button_Click"/>


    </Grid>
</UserControl>
